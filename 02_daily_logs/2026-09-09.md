# Neuro-TMR Daily Log — 2026-09-09

## Project State

Phase II remains active under the frozen architecture:

- **Path A** — asynchronous external expert review
- **Path E** — asynchronous collaboration / opportunity execution
- **Path V** — Validation-Lite execution

Project principle remains:

> **Evidence before engineering.**

Pre-Path V remains complete. Path V is active.

---

## Path E — OPP-039 Engineering City / EIF

A substantive positive response was received from **Arpine Davtyan** after the 2026-09-04 Engineering City / EIF collaboration inquiry was internally routed.

Directly established from the correspondence:

- the institutional contact route worked;
- the inquiry was forwarded internally;
- Arpine Davtyan replied directly;
- she requested a mobile number;
- she stated that she would call to arrange a meeting and discuss possible collaboration in more detail.

A concise reply was prepared and sent with the mobile number.

Current OPP-039 state:

> **IN_PROGRESS — MEETING ARRANGEMENT**

Important boundary:

- no meeting date/time is yet confirmed;
- no technical partnership is claimed;
- exact EEG hardware, streaming/API capability, synchronization, sleep-specific acquisition, PSG/EOG/EMG capability, collaboration mechanism, and later ethics route remain unverified.

Next Path E action:

> **Wait for Arpine's call, capture the meeting context, then prepare specifically for the actual meeting.**

The A6 execution tracker was updated accordingly.

---

## Path V — PV-1 Spectral Feature Pipeline

The reusable spectral feature extractor in:

`src/neuro_tmr/features/spectral.py`

was verified successfully.

Confirmed module contract:

- 6 PSG EEG channels:
  - PSG_F3
  - PSG_F4
  - PSG_C3
  - PSG_C4
  - PSG_O1
  - PSG_O2
- sampling frequency: **256 Hz**
- epoch length: **30 s**
- samples per epoch: **7,680**
- 5 frequency bands:
  - Delta: 0.5–4 Hz
  - Theta: 4–8 Hz
  - Alpha: 8–12 Hz
  - Sigma: 12–16 Hz
  - Beta: 16–30 Hz
- 2 measurements per band:
  - log10 absolute band power
  - relative band power %
- total model features:
  - **6 channels × 5 bands × 2 = 60 features**

The spectral feature test module was implemented:

`tests/test_spectral_features.py`

Synthetic unit tests passed.

---

## Real BOAS Feature Validation

A real N3 reference epoch was processed through the reusable extractor:

- recording: `sub-20`
- participant: `pid 13`
- epoch index: `158`
- human stage: `N3`
- PSG EEG shape: `(6, 7680)`

The extractor produced exactly 60 features and reproduced the independently calculated spectral values.

Example:

- PSG_F3 Delta relative power: **93.015544%**
- PSG_O2 Delta relative power: **89.517692%**

Conclusion:

> The PV-1 ingestion-to-spectral-feature workflow is validated at the component and real-epoch level.

---

## Real Tabularization Check

A three-row real BOAS mini-table was constructed directly from real epochs.

Result:

- shape: **(3, 65)**
- metadata / target columns: **5**
- model feature columns: **60**
- missing values: **0**

This verified the full transformation:

```text
EDF
↓
BOAS recording
↓
30-second epoch
↓
6 × 7680 raw EEG
↓
Welch PSD
↓
60 spectral features
↓
65-column tabular ML row
```

---

## PV-1 Dataset Module

A dedicated dataset-construction module was created:

`src/neuro_tmr/datasets/pv1.py`

The frozen PV-1 cohort was encoded explicitly.

### Train

- `sub-20 / pid 13` → 1048 standard epochs
- `sub-22 / pid 14` → 1034
- `sub-107 / pid 75` → 952

Train total:

> **3034 epochs**

### Validation

- `sub-128 / pid 108`
- 821 total epochs
- 820 standard epochs

Validation total:

> **820 epochs**

### Test

- `sub-29 / pid 21` → 1108 standard epochs

Test total:

> **1108 epochs**

### Full PV-1

> **4962 standard epochs**

Expected stage counts frozen in code:

- Wake: 770
- N1: 179
- N2: 2992
- N3: 279
- REM: 742

Expected table schema:

> **4962 rows × 65 columns**

---

## Controlled Full-Recording Stress Test

The dataset builder was stress-tested on `sub-128 / pid 108`, the only PV-1 recording containing one non-standard label.

Result:

- total epochs: 821
- standard rows built: 820
- shape: `(820, 65)`
- stage counts matched exactly
- participant identity preserved
- split preserved as validation
- duplicate epoch identities: 0
- feature matrix shape: `(820, 60)`
- all features finite: True
- missing table values: 0

Conclusion:

> The builder correctly excludes the non-standard epoch while preserving original BOAS epoch indices.

---

## Canonical PV-1 Dataset Build

A dedicated execution script was created:

`scripts/build_pv1_dataset.py`

Its responsibility is to:

1. build the frozen five-recording PV-1 feature table;
2. validate it in memory;
3. save a canonical Parquet artifact;
4. save a human-readable CSV copy;
5. reload and validate the Parquet artifact;
6. write a provenance / integrity manifest.

`pyarrow` was installed and verified.

Versions:

- pandas: **3.0.5**
- pyarrow: **25.0.1**

The full canonical PV-1 build completed successfully.

Final result:

- rows: **4962**
- columns: **65**
- model features: **60**
- train: **3034**
- validation: **820**
- test: **1108**
- stage counts:
  - Wake: 770
  - N1: 179
  - N2: 2992
  - N3: 279
  - REM: 742
- in-memory validation: **PASS**
- Parquet round-trip validation: **PASS**

Runtime on current CPU:

> approximately **6 min 13 s**

Canonical artifacts were written to:

`~/neuro-tmr-data/derived/pv1/`

Files:

- `pv1_feature_table.parquet`
- `pv1_feature_table.csv`
- `pv1_manifest.json`

The Parquet file is the canonical future modeling input.

The CSV is the human-readable inspection copy.

The manifest records provenance and integrity information.

---

## Independent Dataset Audit

An independent post-build audit of the saved dataset was completed successfully.

This was the final dataset-engineering gate before baseline modeling.

Current formal state:

> **PV-1 DATA CONSTRUCTION — COMPLETE**

> **PV-1 CANONICAL MODELING DATASET — BUILT, AUDITED, AND FROZEN**

No Neuro-TMR model has been trained yet.

No model-performance claim exists yet.

---

## End-of-Day Exact Stop Point

Path V is paused at the clean boundary:

```text
DATA ENGINEERING
    COMPLETE

----------------------------

BASELINE MODELING
    NOT STARTED
```

The next Path V phase will begin from the frozen Parquet dataset rather than reopening the EDF files during normal model development.

Path E continues asynchronously while waiting for Arpine Davtyan's call and any other external responses.
