# Neuro-TMR Daily Log — 2026-09-10

## Starting State

Path V resumes from:

> **PV-1 canonical modeling dataset built, independently audited, and frozen.**

Canonical input:

`~/neuro-tmr-data/derived/pv1/pv1_feature_table.parquet`

Dataset structure:

- 4962 rows
- 65 total columns
- 60 model features
- 5 metadata / target columns

Frozen split:

- train: 3034 epochs — pids 13, 14, 75
- validation: 820 epochs — pid 108
- test: 1108 epochs — pid 21

No baseline classifier has been trained yet.

Path E remains asynchronous.

OPP-039 remains:

> **IN_PROGRESS — MEETING ARRANGEMENT**

Wait for Arpine Davtyan's call without blocking Path V.

---

## Primary Goal for 2026-09-10

> **Begin the first PV-1 baseline-model pipeline using the frozen canonical feature dataset.**

The goal is not to maximize performance immediately.

The goal is to establish a correct, interpretable, leakage-safe baseline and understand the behavior of the data and model.

---

## Planned Path V Work

### 1. Define the baseline-model contract before fitting

Freeze explicitly:

- canonical dataset path;
- exact 60 feature columns;
- target = `stage_hum`;
- metadata columns excluded from model input;
- participant-held-out split remains unchanged;
- validation used for model-development decisions;
- test remains untouched until the baseline design is frozen;
- no neighbor/future epoch context;
- no EOG/EMG/headband features;
- no deep architecture search.

### 2. Inspect model-ready split objects

Load the frozen Parquet artifact and construct:

- `X_train`
- `y_train`
- `X_validation`
- `y_validation`
- `X_test`
- `y_test`

Verify:

- shapes;
- feature ordering;
- stage counts;
- participant membership;
- no leakage;
- no missing or non-finite values.

### 3. Decide preprocessing / scaling

Determine whether the first classifier requires scaling.

If scaling is used:

> fit preprocessing on TRAIN only.

Validation and test must only receive transformations learned from train.

No information from validation/test may influence training preprocessing.

### 4. Freeze the first simple classifier

Select one simple, interpretable baseline classifier.

The first model should be chosen for:

- correctness;
- interpretability;
- CPU feasibility;
- class-probability / decision analysis where useful;
- suitability for 60 tabular spectral features.

Do not perform broad model search.

### 5. Define evaluation metrics before fitting

Primary evaluation should include:

- macro-F1;
- per-stage precision;
- per-stage recall;
- per-stage F1;
- confusion matrix;
- explicit N3 precision;
- explicit N3 recall;
- explicit N3 false-positive behavior.

Overall accuracy may be reported but must not be treated as sufficient because N2 dominates the cohort and N3 is a minority intervention-relevant class.

### 6. Address class imbalance carefully

Training distribution is naturally imbalanced.

Do not rebalance validation or test.

If class weighting is used, it must be justified and applied only as a training decision.

Avoid introducing multiple simultaneous changes that make the first baseline difficult to interpret.

### 7. Train the first baseline on TRAIN only

Fit the first baseline model only after the above contract is frozen.

Do not inspect final test performance during initial model construction.

### 8. Evaluate first on VALIDATION

Use validation to understand:

- stage-wise behavior;
- N3 behavior;
- major confusions;
- whether the pipeline is functioning sensibly;
- whether one justified baseline refinement is needed.

### 9. Protect TEST

The test participant (`pid 21 / sub-29`) remains held out until the baseline configuration is finalized.

The test result should be treated as a one-participant PV-1 pilot result, not as a final BOAS-wide estimate.

---

## Scientific Guardrails

PV-1 remains:

> **a small-cohort engineering / modeling pilot**

It is not:

- final BOAS performance;
- broad-population validation;
- reduced-channel validation;
- headband validation;
- real-time validation;
- cue-delivery validation;
- commercial validation.

The current model uses:

> **six-channel PSG EEG spectral features only**

Later comparisons with reduced PSG EEG or headband EEG must be treated as separate experiments.

---

## Expected End-of-Day Target

A successful 2026-09-10 Path V session should ideally end with:

1. a frozen baseline-model contract;
2. a clean model-ready train/validation/test interface;
3. the first simple classifier implemented;
4. first validation metrics produced and interpreted;
5. no leakage;
6. test still protected unless the baseline configuration is explicitly finalized.

Do not chase performance before understanding the first baseline.
